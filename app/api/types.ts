export interface MovePropType {
  floor: number;
  classroom_name: string;
}

export interface OutPropType {
  reason: string;
  start_time: string;
  end_time: string;
}

export interface ReturnPropType {
  reason: string;
  start_time: string;
}

export interface loginPropType {
  account_id: string;
  password: string;
}
